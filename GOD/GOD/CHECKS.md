# CHECKS

Build/Run OK
Behavior correct
No new errors
Docs updated when required
Simulation loop runs without uncaught exceptions or suppressed errors.
Rendering follows documented data flow; no simulation side effects.
Input handling stays deterministic and idempotent per frame.
Save/load preserves compatibility or is explicitly versioned.
No perf regression without documented, measured justification.
git diff --check is clean before any push.
CHANGELOG.md entries exist for planned and implemented work.
Report lists files changed plus verification results (or explicit note if not run).
